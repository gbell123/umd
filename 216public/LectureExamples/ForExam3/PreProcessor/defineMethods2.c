#define SIZE 20
#include <stdio.h>

/* compile with no extra options and run
   and then with -D SIZE=100 
*/
int main(){
   printf("size = %d\n",SIZE);
   printf("size2 = %d\n",SIZE2);
   return 0;
}
